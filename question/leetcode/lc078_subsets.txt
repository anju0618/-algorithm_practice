lc078_subsets
Based on: LeetCode 78. Subsets

Assignment (paraphrased)
Given an array of unique integers, return all possible subsets (the
power set), with no duplicate subsets. Order doesn't matter.

Function signature
def subsets(nums: list[int]) -> list[list[int]]:

Examples
Input
subsets([1,2,3])
Output (order doesn't matter)
[[],[1],[2],[1,2],[3],[1,3],[2,3],[1,2,3]]
Input
subsets([0])
Output
[[],[0]]

NeetCode 150 category: Backtracking
