lc226_invert_binary_tree
Based on: LeetCode 226. Invert Binary Tree

Assignment (paraphrased)
Given the root of a binary tree, invert it (swap every left/right
child pair) and return the root.

Class/function signature
class TreeNode:
    def __init__(self, val=0, left=None, right=None): ...
def invert_tree(root: TreeNode | None) -> TreeNode | None:

Examples
Input (level-order, None = missing node)
invert_tree(tree from [4,2,7,1,3,6,9]) then read level-order
Output
[4,7,2,9,6,3,1]
Input
invert_tree(tree from [2,1,3]) then read level-order
Output
[2,3,1]
Input
invert_tree(tree from [])
Output
None

NeetCode 150 category: Trees
