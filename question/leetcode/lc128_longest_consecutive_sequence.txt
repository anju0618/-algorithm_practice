lc128_longest_consecutive_sequence
Based on: LeetCode 128. Longest Consecutive Sequence

Assignment (paraphrased)
Given an unsorted array of integers, return the length of the longest
run of consecutive integers that appear in the array (order in the
input doesn't matter). Must run in O(n) time.

Function signature
def longest_consecutive(nums: list[int]) -> int:

Examples
Input
longest_consecutive([100,4,200,1,3,2])
Output
4
Input
longest_consecutive([0,3,7,2,5,8,4,6,0,1])
Output
9
Input
longest_consecutive([1,0,1,2])
Output
3

NeetCode 150 category: Arrays & Hashing
