lc261_graph_valid_tree
Based on: LeetCode 261. Graph Valid Tree (premium-only problem; this is
a paraphrase from general knowledge, not fetched from LeetCode's API
since the official text is behind a paywall)

Assignment (paraphrased)
Given n nodes labeled 0..n-1 and a list of undirected edges, return
True if these edges form a valid tree (connected, no cycles).

Function signature
def valid_tree(n: int, edges: list[list[int]]) -> bool:

Examples
Input
valid_tree(5, [[0,1],[0,2],[0,3],[1,4]])
Output
True
Input
valid_tree(5, [[0,1],[1,2],[2,3],[1,3],[1,4]])
Output
False
