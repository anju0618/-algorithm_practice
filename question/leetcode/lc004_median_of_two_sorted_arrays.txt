lc004_median_of_two_sorted_arrays
Based on: LeetCode 4. Median of Two Sorted Arrays

Assignment (paraphrased)
Given two sorted arrays, return the median of the combined data.
Must run in O(log(m+n)) time.

Function signature
def find_median_sorted_arrays(nums1: list[int], nums2: list[int]) -> float:

Examples
Input
find_median_sorted_arrays([1,3], [2])
Output
2.0
Input
find_median_sorted_arrays([1,2], [3,4])
Output
2.5

NeetCode 150 category: Binary Search
