lc136_single_number
Based on: LeetCode 136. Single Number

Assignment (paraphrased)
Given a non-empty array where every element appears twice except one,
find that single element. Must run in linear time using only constant
extra space.

Function signature
def single_number(nums: list[int]) -> int:

Examples
Input
single_number([2,2,1])
Output
1
Input
single_number([4,1,2,1,2])
Output
4

NeetCode 150 category: Bit Manipulation
